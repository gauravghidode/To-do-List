<%- include("header") -%>

    <!-- <% if(listTitle==="weekday"){ %>
        <h1 style="color: purple">It is a <%= listTitle %></h1>
   <% }else{ %>
        <h1 style="color: red">It is a <%= listTitle %></h1>
   <% } %> -->
    <div class="box" id="heading">
        <h1 style="">
            <%= listTitle %>
        </h1>
    </div>

    <div class="box">
        <% for(var i=0; i< item.length; i++){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%= item[i]._id %>" onchange="this.form.submit()">
                    <p>
                        <%= item[i].name %>
                    </p>
                </div>
                <input type="hidden" name="listTitle" value="<%= listTitle %>">
            </form>
            <% } %>

                <form action="/" method="post">
                    <input type="text" name="newItem" id="" placeholder="New Item" autocomplete="off">
                    <button type="submit" name="list" value=<%=listTitle %> >+</button>
                    <!--we use this name list in post method -->
                </form>
    </div>

    <%- include("footer") -%>